import { Command } from 'commander';
/**
 * Creates the 'compile' command that converts IR to Svelte or Vue.
 */
export declare function createCompileCommand(): Command;
//# sourceMappingURL=compile.d.ts.map